spring:
  application:
    name: microservice-geo

  cloud:
    kubernetes:
      secrets:
        sources:
          - name: microservice-geo
      config:
        sources:
          - name: library-core
          - name: microservice-geo
      reload:
        enabled: true

management:
  endpoint:
    restart:
      enabled: true
    health:
      enabled: true
    info:
      enabled: true
    prometheus:
      enabled: true
